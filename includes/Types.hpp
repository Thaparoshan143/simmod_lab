// all the types are defined in this standalone header file 
#pragma once

#include <iostream>
#include <vector>
#include <string>

using uint = unsigned int;
using String = std::string;

template<class T>
using Vec = std::vector<T>;

